<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% messages.forEach((mesaage, index) => { %> 
      <li>
        <strong><%= mesaage.user %>:</strong> <%= mesaage.text %> <em>(<%= mesaage.added.toLocaleString() %>)</em>
        <a href="/views/message.ejs/<%= index %>"></a>
      </li>
    <% }) %>
  </ul>
  <a href="/new">Add a new message</a>
</body>
</html>